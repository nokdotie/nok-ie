name: Ping

on:
  workflow_dispatch:
  schedule:
    - cron: '0 * * * *'

concurrency:
  group: ${{ github.workflow }}
  cancel-in-progress: true

jobs:
  ping:
    name: Ping
    runs-on: ubuntu-latest

    steps:
      - name: Ping
        run: curl -sfLXGET https://nok.ie
